---

---

<section class="campaign">
    <h2>Limited time only</h2>
    <p>Sign up to our newsletter and get an <span>exclusive goodiebag</span>  next time you shop at Omada Wine & Deli</p>

    <form action="">
        <div class="form-fields">
            <input type="text" name="name" placeholder="name..." />
            <input type="email" name="email" placeholder="e-mail..." />
        </div>
      <input type="submit" value="subscribe" />
    </form>
    <p class="banner-bottom-text">Only at in-store purchase over 200 DKK. The goodie-bag has a value of 450 DKK</p>
    <div class="background">
        <div class="splash-1"></div>
    </div>
</section>

  <style>

    section {
        overflow: hidden;
    }

    .campaign {
        padding-block: 2rem;
        padding-inline: 2rem;
        background-color: var(--secondary-background-color);
        color: var(--background-color);
    }

    .background {
   

    }

    .splash-1 {
        background: url("omadasplash.webp");
        background-size: cover;
        background-position: center;
        width: 100%;
        height: 100%;
        top: 0;
        left: 0;
        position: relative;
    }

    h2 {
        font-weight: 600;
        letter-spacing: 0.08em;
        font-size: 2.5rem;
        text-align: center;
    }

    p {
        text-align: center;
        margin-inline: auto;
        font-weight: 100;
    }

    span {
        font-weight: 600;
    }

    .banner-text {
        margin-block: 1rem;
        max-width: 30%;
    }

    .bottom-text {
            font-size: 0.7rem;
            margin-top: 2rem;
    }

    form {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        width: 60%;
        margin-inline: auto;
    }

    .form-fields {
        display: flex;
        flex-direction: row;
        gap: 1rem;
        margin: 0 auto;
    }

    input {
        font-family: var(--heading-font);
        background-color: transparent;
        color: var(--background-color);
        border: solid 1px;
        padding: 0.5rem 1rem;
    }

    input::placeholder {
        color: var(--background-color);
    }

    input[type="submit"] {
        border-bottom: none;
        background-color: var(--background-color);
        color: #264f48;
        width: fit-content;
        padding: 0.5rem 1rem;
        margin-inline: auto;
        margin-bottom: 2rem;
        letter-spacing: 0.08em;
    }

    @media (min-width: 700px) {
        

    }



  </style>